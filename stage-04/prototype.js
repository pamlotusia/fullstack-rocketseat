/*
   Prototype

   * prototype-based language
   * prototype chain
   * __proto__
*/

//basicamente os tipos de dados (string, object, number...) todos são prototipos e possuem seus metodos. Ou seja, na linguagem Javascript quando voce instancia um objeto de qualquer tipo voce consegue usar os metodos nativos dele

console.log('hello'.__proto__) //rodando no devtools ele devolve a lista de propriedades e metodos que podem ser usados para aquela string.